<local:IocControl x:Class="PortraitsDrawing.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PortraitsDrawing.Views"
             xmlns:viewmodels="clr-namespace:PortraitsDrawing.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200" Background="#2C2C50"
                   d:DataContext="{d:DesignInstance viewmodels:HelpViewModel}">
    
    <Grid>
        <Grid.RowDefinitions>
           
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <TextBlock Text="help"/>
        </Grid>
    </Grid>
</local:IocControl>